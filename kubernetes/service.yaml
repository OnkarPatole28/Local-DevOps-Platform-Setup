apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-app
  name: my-app
spec:
  type: NodePort
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: my-app
status:
  loadBalancer: {}
